'use strict';

/**
 * seasonal-deal-section-title router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::seasonal-deal-section-title.seasonal-deal-section-title');
