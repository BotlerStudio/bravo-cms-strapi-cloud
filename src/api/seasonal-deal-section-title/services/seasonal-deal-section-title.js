'use strict';

/**
 * seasonal-deal-section-title service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::seasonal-deal-section-title.seasonal-deal-section-title');
