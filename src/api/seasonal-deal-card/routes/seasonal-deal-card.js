'use strict';

/**
 * seasonal-deal-card router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::seasonal-deal-card.seasonal-deal-card');
